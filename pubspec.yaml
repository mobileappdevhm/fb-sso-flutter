name: sso
description: Single-Sign-On with Facebook

dependencies:
  url_launcher: ^3.0.0
  http: ^0.11.3+16
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - res/bg.jpg
    - res/cert.pem
    - res/key.pem
